/**
 * Tailwind v4 is a single PostCSS plugin — the design tokens live in CSS
 * (`app/globals.css`, `@theme`) rather than a JS config object.
 */
const config = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
}

export default config
